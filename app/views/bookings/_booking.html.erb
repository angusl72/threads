<div class="d-flex flex-wrap">
  <% @bookings.each do |booking| %>

    <div class="card m-2 text-center" style="width: 18rem;">
      <img src="https://www.legacy.com.au/wp-content/uploads/2020/10/tshirt-2.jpg" class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title"><%= link_to booking.item.name, edit_booking_path(booking) %></h5>
        <p class="card-text"><strong>Status: </strong><%= booking.status %></p>
        <% days = ((booking.end_date - booking.start_date) / 1.day).to_i %>
        <% total_price = booking.item.price.to_i * days %>
        <p class="card-text"><strong>Start date: </strong><%= booking.start_date.strftime("%d/%m/%Y") %></p>
        <p class="card-text"><strong>End date: </strong><%= booking.end_date.strftime("%d/%m/%Y") %></p>
        <p class="card-text"><strong>Total days: </strong><%= days %></p>
        <p class="card-text"><strong>Earnings: </strong>$<%= total_price %></p>
        <p class="card-text"><strong>User id: </strong><%= booking.user_id %></p>

      </div>
    </div>

  <% end %>

</div>
